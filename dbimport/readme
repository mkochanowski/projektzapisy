dbimport.py:
This python script migrates data from old database (users and semesters
including groups, enrollings etc.).

Before running the script settings must be correctly adjusted inside the file.
This script is using ./settings.py (. - Fereol directory) to set up django
environment.

Usage:
python dbimport.py
