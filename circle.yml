general:
  build_dir: zapisy

machine:
  python:
    version: 2.7.10

dependencies:
  override:
    - pip install -r requirements.test.txt

test:
  override:
    - mkdir logs
    - python manage.py test test_app
    - python manage.py test news
    - python manage.py test schedule
    - python manage.py test courses
    - python manage.py test vote
